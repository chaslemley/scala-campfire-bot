package campfire

object CampfireBot {
  def main(args:Array[String]) {
    // // val user = User(705492)

    // // println(user)
    // // println(room)

    // val recent_messages = room.recentMessages

    // println(recent_messages(0).`type`)

    val room = Room(260694)
    // val bot = Bot()
    // bot.speak(room, "This is a test")
  }
}